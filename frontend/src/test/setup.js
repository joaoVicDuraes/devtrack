// Global test setup, run once before the suite (referenced by vite.config.js).
// Importing jest-dom registers custom matchers like toBeInTheDocument() and
// toHaveTextContent() so component tests can assert on the rendered DOM.
import '@testing-library/jest-dom';
